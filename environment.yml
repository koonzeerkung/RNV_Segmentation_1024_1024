name: rnv
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch
      - torchvision
      - segmentation-models-pytorch
      - timm
      - albumentations
      - opencv-python-headless
      - numpy
      - scipy
      - pandas
      - matplotlib
      - tqdm
      - pillow
